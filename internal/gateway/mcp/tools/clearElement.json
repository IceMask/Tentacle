{
  "name": "clearElement",
  "description": "清空输入框元素的内容",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "会话ID"
      },
      "elementId": {
        "type": "string",
        "description": "元素ID，由 findElement 返回"
      }
    },
    "required": ["sessionId", "elementId"],
    "additionalProperties": false
  }
}
