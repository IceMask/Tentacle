{
  "name": "endSession",
  "description": "Closes an active mobile testing session and releases all associated resources including the app instance and device connection. Always call this when testing is complete to avoid resource leaks. The sessionId becomes invalid after this operation.",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "The session identifier returned by startSession that you want to terminate"
      }
    },
    "required": ["sessionId"],
    "additionalProperties": false
  }
}
