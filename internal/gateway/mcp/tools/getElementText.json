{
  "name": "getElementText",
  "description": "获取元素的可见文本内容",
  "inputSchema": {
    "type": "object",
    "properties": {
      "sessionId": {
        "type": "string",
        "description": "会话ID"
      },
      "elementId": {
        "type": "string",
        "description": "元素ID，由 findElement 返回"
      }
    },
    "required": ["sessionId", "elementId"],
    "additionalProperties": false
  }
}
