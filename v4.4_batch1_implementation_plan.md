# v4.4_batch1_implementation_plan.md — v4.4 第一批实现任务与改表清单

> **状态**：Draft  
> **对应文档**：`requirements.v4.4.md`、`module_v4.4_design.md`  
> **目标**：把 v4.4 中已经拍板的设计拆成第一批可执行任务，并明确本批次真正需要落地的 schema 变化。

## 1. 本批次目标

本批次只解决四类高优先级问题：

1. 让鉴权模型从“代码存在但契约不完整”变成“主路径可运行”。
2. 让 distributed 模式从“accepted 后可能挂死”变成“有 lease、有 orphan 收口”。
3. 让浏览器 WebSocket 订阅从“只有入口”变成“有安全模型”。
4. 让安全与审计从“理念存在”变成“有最小可验证闭环”。

本批次不追求一次性做完全部治理能力，而是优先把最容易导致错误行为、错误暴露面和错误状态收敛的问题先做实。

## 2. 本批次范围

### 2.1 In Scope

- PAT 正式模式落地
- OIDC 配置收敛
- HMAC key registry 与 canonical request 收敛
- Worker 状态机落地
- execution lease 运行态落地
- orphan trace 收口与迟到结果防覆盖
- 浏览器 WebSocket subscription token
- WebSocket origin 策略
- 外部 TLS 强制策略
- 审计模型扩展
- 对应单测、集成测试与迁移

### 2.2 Out of Scope

- 内部 gRPC 完整 mTLS
- KMS/HSM 深度集成
- PAT 管理后台或控制台
- HMAC key 自助轮换 API
- WebSocket 第二种浏览器 token 传递方式
- Module II 的集中治理 API

## 3. 改表清单

本批次建议只做“最小但够用”的 schema 变更，避免把仍可由运行态表达的概念过早固化。

### 3.1 必做 Migration

#### 3.1.1 `003_traces_execution_state.sql`

目标：让 trace 当前 attempt 和终态原因可持久化，避免 distributed 执行控制完全依赖 Redis 运行态。

建议变更：

- `traces.current_attempt INTEGER NOT NULL DEFAULT 0`
- `traces.terminal_reason TEXT NULL`
- `traces.last_worker_id TEXT NULL`

说明：

- `current_attempt` 用于迟到结果校验与 orphan 收口。
- `terminal_reason` 用于区分 `failed` 是业务失败、timeout、orphaned 还是其他原因。
- `last_worker_id` 不是 ownership 权威字段，只用于排障、审计和健康输出。

建议索引：

- `idx_traces_project_status_attempt_updated_at (project_id, status, current_attempt, updated_at DESC)`

#### 3.1.2 `004_pat_tokens.sql`

目标：提供 PAT 的正式权威源。

建议新增表 `pat_tokens`：

| 字段 | 类型建议 | 说明 |
| --- | --- | --- |
| `token_id` | `TEXT PRIMARY KEY` | token 主键 |
| `tenant_id` | `TEXT NOT NULL` | 租户归属 |
| `subject_id` | `TEXT NOT NULL` | 主体归属 |
| `display_name` | `TEXT NOT NULL` | 展示名 |
| `secret_hash` | `TEXT NOT NULL` | secret 哈希 |
| `status` | `TEXT NOT NULL` | `active` / `rolling` / `revoked` |
| `scopes_json` | `BYTEA NOT NULL` | scope JSON |
| `expires_at` | `TIMESTAMPTZ NULL` | 过期时间 |
| `created_at` | `TIMESTAMPTZ NOT NULL` | 创建时间 |
| `created_by` | `TEXT NOT NULL` | 创建者 |
| `revoked_at` | `TIMESTAMPTZ NULL` | 吊销时间 |
| `revoked_by` | `TEXT NULL` | 吊销者 |
| `rotated_from` | `TEXT NULL` | 前一代 token_id |
| `last_used_at` | `TIMESTAMPTZ NULL` | 最近使用时间 |
| `last_used_ip` | `TEXT NULL` | 最近使用 IP |
| `metadata` | `BYTEA NOT NULL` | 预留扩展 JSON |

建议索引：

- `idx_pat_tokens_tenant_status_created_at (tenant_id, status, created_at DESC)`
- `idx_pat_tokens_subject_status_created_at (subject_id, status, created_at DESC)`
- `idx_pat_tokens_rotated_from (rotated_from) WHERE rotated_from IS NOT NULL`

#### 3.1.3 `005_hmac_keys.sql`

目标：提供 HMAC key registry。

建议新增表 `hmac_keys`：

| 字段 | 类型建议 | 说明 |
| --- | --- | --- |
| `key_id` | `TEXT PRIMARY KEY` | key 主键 |
| `tenant_id` | `TEXT NOT NULL` | 租户归属 |
| `display_name` | `TEXT NOT NULL` | 展示名 |
| `secret_ref` | `TEXT NOT NULL` | 密钥引用或密文 |
| `status` | `TEXT NOT NULL` | `active` / `rolling` / `retired` / `revoked` |
| `not_before` | `TIMESTAMPTZ NULL` | 生效时间 |
| `not_after` | `TIMESTAMPTZ NULL` | 停止接受时间 |
| `created_at` | `TIMESTAMPTZ NOT NULL` | 创建时间 |
| `created_by` | `TEXT NOT NULL` | 创建者 |
| `revoked_at` | `TIMESTAMPTZ NULL` | 吊销时间 |
| `metadata` | `BYTEA NOT NULL` | 预留扩展 JSON |

建议索引：

- `idx_hmac_keys_tenant_status_created_at (tenant_id, status, created_at DESC)`
- `idx_hmac_keys_tenant_validity (tenant_id, not_before, not_after)`

#### 3.1.4 `006_audit_logs_v44.sql`

目标：把现有 `audit_logs` 从“自由 payload”提升到“可查询的统一审计模型”。

建议变更：

- `audit_logs.actor_id TEXT NULL`
- `audit_logs.actor_type TEXT NULL`
- `audit_logs.auth_scheme TEXT NULL`
- `audit_logs.credential_id TEXT NULL`
- `audit_logs.tenant_id TEXT NULL`
- `audit_logs.result TEXT NULL`
- `audit_logs.reason TEXT NULL`
- `audit_logs.source_ip TEXT NULL`
- `audit_logs.request_id TEXT NULL`
- `audit_logs.metadata BYTEA NULL`

说明：

- 保留现有 `actor` 与 `payload` 一段迁移期，避免一次性重写全量读写路径。
- 新写入路径优先写新字段；旧字段在兼容窗口内继续保留。

建议索引：

- `idx_audit_logs_tenant_created_at (tenant_id, created_at DESC)`
- `idx_audit_logs_credential_created_at (credential_id, created_at DESC) WHERE credential_id IS NOT NULL`
- `idx_audit_logs_request_id (request_id) WHERE request_id IS NOT NULL`

### 3.2 本批次不新增 SQL 表的项

#### 3.2.1 不新增 `execution_leases` SQL 表

推荐理由：

- execution lease 本质是运行态 ownership，最适合用 Redis TTL 表达。
- 本批次只需要保证 orphan 收口和 attempt 防覆盖，不需要先引入另一张高频更新的 SQL 表。
- 只要把 `traces.current_attempt` 和 `terminal_reason` 持久化，配合 Redis lease key，就足以支撑第一批闭环。

推荐 Redis key 形态：

- `execution_lease:{trace_id}`

推荐 value 字段：

- `worker_id`
- `attempt`
- `lease_expires_at`
- `last_renewed_at`

## 4. 第一批实现任务

### 4.1 Schema / Persistence

#### B1-1 新增 traces 执行状态字段

目标：

- 持久化 `current_attempt`
- 持久化 `terminal_reason`
- 记录最近处理该 trace 的 worker

主要改动：

- `internal/storage/postgres/migrations/003_traces_execution_state.sql`
- `internal/storage/postgres/models.go`
- `internal/storage/postgres/dao.go`
- `internal/storage/postgres/coordination.go`

验收点：

- trace 查询可读出当前 attempt 与终态原因
- existing DAO 不会因为新字段破坏现有路径

#### B1-2 新增 PAT / HMAC 权威表

目标：

- 落地 `pat_tokens`
- 落地 `hmac_keys`

主要改动：

- `internal/storage/postgres/migrations/004_pat_tokens.sql`
- `internal/storage/postgres/migrations/005_hmac_keys.sql`
- 新增 DAO 查询接口

验收点：

- 可按 `token_id` 查询 PAT
- 可按 `key_id` 查询 HMAC key

#### B1-3 扩展 audit_logs

目标：

- 让 audit 查询不再只能靠解析 payload

主要改动：

- `internal/storage/postgres/migrations/006_audit_logs_v44.sql`
- `internal/storage/postgres/coordination.go`
- audit model 与写入 helper

验收点：

- 审计日志可按 `tenant_id`、`credential_id`、`request_id` 直接检索

### 4.2 Gateway / Auth

#### B1-4 PAT 正式接线

目标：

- PAT 不再依赖静态临时 lookup

主要改动：

- `internal/auth/pat.go`
- 新增 PAT repository / cache helper
- `internal/gateway/middleware/auth.go`
- `cmd/gateway/main.go`
- `internal/config/config.go`
- `internal/config/loader.go`

验收点：

- `auth.pat.mode=postgres` 时，Bearer PAT 可在网关主路径完成认证
- `auth.pat.mode=disabled` 时，PAT 路径不被激活

#### B1-5 OIDC 配置收敛

目标：

- 从 `jwks_url` 语义切换到 `issuer_url + audience`

主要改动：

- `internal/config/config.go`
- `internal/config/loader.go`
- `internal/auth/oidc.go`
- `cmd/gateway/main.go`

验收点：

- 启动校验要求 `issuer_url + audience`
- 兼容旧配置时有明确 fallback 或报错说明

#### B1-6 HMAC 正式接线

目标：

- HMAC 走 key registry
- canonical request 覆盖 query

主要改动：

- `internal/auth/hmac.go`
- `internal/gateway/middleware/auth.go`
- `internal/gateway/rest/router.go`
- `cmd/gateway/main.go`

验收点：

- query 参数变化会导致签名失效
- nonce key 带 `tenant_id + key_id`

#### B1-7 鉴权冲突处理

目标：

- 明确多凭证冲突行为

主要改动：

- `internal/gateway/middleware/auth.go`
- `internal/errors/codes.go`
- JSON-RPC / REST 错误映射

验收点：

- 同一请求携带多套完整凭证时稳定返回鉴权冲突错误

### 4.3 Distributed Execution

#### B1-8 Worker 状态机落地

目标：

- 把 `healthy / degraded / offline / draining` 变成正式状态机

主要改动：

- `internal/orchestrator/worker_registry.go`
- `internal/orchestrator/health.go`

验收点：

- miss / recover / drain 都有明确状态迁移
- `healthCheck` 可输出 worker 状态摘要

#### B1-9 Execution lease 运行态落地

目标：

- accepted 之后必须产生 lease
- worker 心跳与 lease 续约解耦但协同

主要改动：

- `internal/orchestrator/dispatcher.go`
- `internal/orchestrator/service.go`
- `internal/storage/redis/cache.go`

验收点：

- distributed trace 在 worker accepted 后一定存在 lease key
- lease 过期后 watchdog 能识别 orphan 候选

#### B1-10 Orphan 收口与迟到结果防覆盖

目标：

- orphan trace 不再无限挂起
- 旧 attempt 结果无法覆盖当前状态

主要改动：

- `internal/orchestrator/persistence.go`
- `internal/orchestrator/service.go`
- worker 结果回传路径

验收点：

- step 未开始时 orphan 可重试
- step 已开始时 orphan 默认 failed
- 迟到结果被拒绝覆盖并写审计

### 4.4 WebSocket / TLS / Audit

#### B1-11 WebSocket subscription token

目标：

- 浏览器端不直接用长期 Bearer 建立 WS

主要改动：

- `internal/gateway/websocket/*`
- `internal/gateway/rest/router.go`
- `cmd/gateway/main.go`
- token 生成/校验 helper

验收点：

- `POST /api/v1/traces/{id}:subscribe` 可签发短时 token
- 无 token 或 trace 不匹配时拒绝握手

#### B1-12 外部 TLS 与安全健康项

目标：

- 生产环境外部入口强制 TLS
- 健康检查暴露安全维度

主要改动：

- `cmd/gateway/main.go`
- `internal/startup/preflight.go`
- `internal/orchestrator/health.go`

验收点：

- 生产模式明文启动被拒绝
- `/healthz` 可见 `external_tls_enabled`、`rpc_security_mode`、`auth_failure_rate`

#### B1-13 审计统一接线

目标：

- 关键安全与 distributed 事件统一写审计

主要改动：

- auth middleware
- subscription token 签发路径
- worker register / offline / recover
- orphan finalized / late result rejected

验收点：

- 至少能查到 auth success/failure、lease expired、WS subscribe、orphan finalized

## 5. 建议实施顺序

### 5.1 第 1 周

- B1-1
- B1-2
- B1-3
- B1-4
- B1-5

### 5.2 第 2 周

- B1-6
- B1-7
- B1-8
- B1-9
- B1-10

### 5.3 第 3 周

- B1-11
- B1-12
- B1-13

## 6. 验收清单

- 所有 migration 可重复执行
- `go test ./...` 通过
- 至少存在以下新增测试：
  - PAT / OIDC / HMAC 认证成功与失败
  - 多凭证冲突
  - worker 状态迁移
  - accepted-no-result orphan
  - late result rejected
  - WebSocket subscription token 握手
  - 外部 TLS 强制与健康项输出
- `requirements.v4.4.md`、`module_v4.4_design.md`、实现与测试口径一致

## 7. 当前推荐的首批 issue 切分

建议先开 8 个 issue，而不是 13 个很细的 issue，避免管理成本过高：

1. `v4.4-schema-core`
2. `v4.4-pat-auth`
3. `v4.4-oidc-config`
4. `v4.4-hmac-canonical-request`
5. `v4.4-worker-state-machine`
6. `v4.4-execution-lease-and-orphan`
7. `v4.4-websocket-subscription-security`
8. `v4.4-tls-health-audit`
